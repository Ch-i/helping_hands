# Consolidated Plans: 2026-03-10

Plans completed on 2026-03-10.

---

## v104 - Dead code cleanup and weekly consolidation

Removed documented dead code from 4 source modules, consolidated Week 10 daily plans, updated tech-debt tracker and quality docs, added 12 new tests (11 doc structure + 1 codex sandbox), fixed pre-existing Claude default model test regression.

**Source changes:**
- `codex.py`: Removed redundant `if not sandbox_mode` guard (always-truthy after env/auto resolution)
- `goose.py`: Removed unreachable `if not model` fallback (model always non-empty after strip)
- `e2e.py`: Removed always-true `if final_pr_number is not None` guard (always set in non-dry-run)
- `iterative.py`: Removed unreachable `else: delta = current` branches in AssertionError and awaitable paths (stream_text is always "" at those points); kept if/else in async iter loop where stream_text accumulates
- `web.py`: Marked unreachable latin-1 fallback with `pragma: no cover`

**Doc changes:**
- Consolidated Mar 3-7 daily plans into `docs/exec-plans/completed/2026/Week-10.md`
- Moved 4 dead-code items to Resolved in tech-debt-tracker
- Updated QUALITY_SCORE.md remaining gaps table (8 -> 4 entries)
- Updated testing-methodology.md coverage count (3031 -> 3073)

**Test changes:**
- Added `TestTechDebtResolvedItems` (3 tests: entry count, date format, version refs)
- Added `TestWeeklyConsolidation` (5 tests: Week-10 existence/summary/dates, PLANS.md refs)
- Added `TestQualityScoreDeadCodeCleanup` (2 tests: gaps table, v104 note)
- Added codex sandbox always-populated test
- Fixed `TestPlansTracking` for weekly consolidation (daily files OK unreferenced)
- Fixed `TestPlansMdLinks` chronological order to support weekly format
- Fixed `TestTechDebtTrackerActiveItems` to scope to active section only
- Fixed pre-existing Claude `test_empty_model_returns_empty` -> `test_default_model_returns_opus`

3073 tests (was 3031).

---

## v105 - Server routing completion + E2E draft PR support

Added `docker-sandbox-claude` backend to server routing (Celery `_SUPPORTED_BACKENDS`, `_BACKEND_LOOKUP`, `BackendName`, hand instantiation) and HTTP API (`BuildRequest.backend`). Added E2E draft PR support (`_draft_pr_enabled()` with `HELPING_HANDS_E2E_DRAFT_PR` env var, default=true). Synced frontend `Backend` type and `BACKEND_OPTIONS` (added `docker-sandbox-claude` + `opencodecli`). Updated backend-routing design doc. Resolved 3 tech-debt items (backend routing, E2E draft PR, localStorage polyfill).

**Source changes:**
- `celery_app.py`: Added `docker-sandbox-claude` to `_SUPPORTED_BACKENDS`, `DockerSandboxClaudeCodeHand` import and elif branch
- `app.py`: Added `docker-sandbox-claude` to `BuildRequest.backend` Literal, `BackendName` Literal, and `_BACKEND_LOOKUP`
- `e2e.py`: Added `_draft_pr_enabled()` static method, `create_pr(draft=...)` in `run()`
- `App.tsx`: Added `docker-sandbox-claude` and `opencodecli` to `Backend` type and `BACKEND_OPTIONS`, `backendDisplayName` display name

**Doc changes:**
- Updated `backend-routing.md` (docker-sandbox-claude now in all entry points)
- Updated tech-debt-tracker (3 items resolved: backend routing, E2E draft PR, localStorage polyfill)
- Updated QUALITY_SCORE.md E2E coverage notes
- Updated testing-methodology.md coverage count (3073 -> 3080)

**Test changes:**
- Added `TestNormalizeBackend.test_docker_sandbox_claude_is_supported` (celery_app)
- Added `TestE2EHandDraftPR` (7 tests: default enabled, env false/yes/zero/no, draft=True integration, draft=False integration)
- Updated `TestParseBackend.test_all_known_backends` to include docker-sandbox-claude

3080 tests (was 3073).

---

## v106 - Celery helper extraction and stream collector tests

Extracted `_format_runtime` helper from `build_feature` task for testability. Added comprehensive tests for `_UpdateCollector` edge cases (multi-newline, split lines, buffer boundaries, whitespace, truncation), `_collect_stream` edge cases (empty stream, workspace/started_at forwarding, periodic update frequency), `_format_runtime` (sub-minute, over-minute, zero, boundary), and `_has_gemini_auth` to `test_celery_helpers.py`.

**Source changes:**
- `celery_app.py`: Extracted `_format_runtime(elapsed_seconds)` helper from inline `build_feature` logic

**Doc changes:**
- Updated PLANS.md (active -> completed v106)
- Updated QUALITY_SCORE.md celery_app coverage notes
- Updated testing-methodology.md coverage count (3080 -> 3107)

**Test changes:**
- Added `TestFormatRuntime` to `test_celery_helpers.py` (7 tests: sub-minute, zero, sub-second, exactly-one-minute, minutes-and-seconds, large value, boundary)
- Added `TestHasGeminiAuth` to `test_celery_helpers.py` (4 tests: key set, not set, empty, whitespace)
- Added `TestUpdateCollectorEdgeCases` to `test_celery_helpers.py` (10 tests: multi-newline, newline-only, split-line, feed-after-flush, double-flush, boundary-exact, boundary-then-newline, whitespace-skip, truncation)
- Added `TestCollectStream.test_empty_stream_returns_empty_string` (1 test)
- Added `TestCollectStream.test_workspace_and_started_at_forwarded` (1 test)
- Added `TestCollectStream.test_periodic_updates_called_for_many_chunks` (1 test)
- Added `TestFormatRuntime` to `test_celery_app.py` (3 tests: sub-minute, over-minute, zero)

3107 tests (was 3080).

---

## v107 - Health check tests and tech-debt cleanup

Added comprehensive tests for all three health check endpoints (`/health`, `/health/services`, `/health/claude-usage`) and their underlying helper functions. Resolved stale tech-debt items — Claude Code CLI and Gemini CLI hands are fully implemented with complete test coverage, not scaffold placeholders.

**Source changes:**
- None (test-only and doc-only changes)

**Doc changes:**
- Updated tech-debt-tracker: moved Claude CLI scaffold and Gemini CLI scaffold to Resolved
- Updated QUALITY_SCORE.md server/app.py coverage notes (5 -> 11 test classes)
- Updated testing-methodology.md coverage count (3107 -> 3127)
- Updated PLANS.md (active -> completed v107)

**Test changes:**
- Added `TestHealthEndpoint` (1 test: returns ok)
- Added `TestHealthServicesEndpoint` (5 tests: all-healthy, redis-error, db-na, workers-error, all-degraded)
- Added `TestCheckRedisHealth` (3 tests: ping-ok, ping-fail, import-fail)
- Added `TestCheckDbHealth` (4 tests: no-url, empty-url, connect-ok, connect-fail)
- Added `TestCheckWorkersHealth` (4 tests: workers-respond, no-workers, empty-ping, inspect-raises)
- Added `TestClaudeUsageEndpoint` (3 tests: no-credentials, cached-response, force-bypasses-cache)

3127 tests (was 3107).

---

## v108 - Server helper unit tests

Added direct unit tests for three previously indirectly-tested server helpers: `_get_claude_oauth_token()` (macOS Keychain reader), `_fetch_claude_usage()` (Anthropic OAuth usage API with caching), and `_schedule_to_response()` (ScheduledTask-to-ScheduleResponse conversion). These were only tested through endpoint-level tests before; now all branches are directly covered.

**Source changes:**
- None (test-only and doc-only changes)

**Doc changes:**
- Updated PLANS.md (active -> completed v108)
- Updated QUALITY_SCORE.md server/app.py coverage notes (11 -> 13 test classes)
- Updated testing-methodology.md coverage count (3127 -> 3403)

**Test changes:**
- Added `TestGetClaudeOauthToken` to `test_server_app_usage.py` (9 tests: JSON access token, missing OAuth key, plain JWT, non-JWT, nonzero exit, empty stdout, timeout, generic exception, malformed JSON)
- Added `TestFetchClaudeUsage` to `test_server_app_usage.py` (10 tests: cache hit, cache expired, no token, HTTP error, generic exception, session+weekly success, no usage data, force bypass, session only, cache update)
- Added `TestScheduleToResponse` to `test_server_app_schedule_response.py` (6 tests: enabled with next_run, disabled, all fields forwarded, exception suppressed, missing optional attrs, run count forwarded)

3403 tests (was 3127).

---

## v109 - Add ty type checker to CI

Added `ty check src --ignore unresolved-import --ignore invalid-method-override` step to CI workflow. Fixed 12 type errors across 3 source files to make ty pass clean. Removed 6 stale `type: ignore` comments. Resolved the medium-priority "Type checker not in CI" tech debt item.

**Source changes:**
- `.github/workflows/ci.yml`: Added `Type check` step between ruff and tests
- `e2e.py`: Added `assert final_pr_number is not None` for type narrowing before PR body update
- `celery_app.py`: Typed `overrides` dict as `dict[str, ConfigValue]`, imported `ConfigValue`, removed stale `type: ignore[union-attr]`
- `schedules.py`: Added `assert` type narrowing for `croniter` and `RedBeatSchedulerEntry` after `_check_*()` guards; removed 5 stale `type: ignore[assignment]` comments

**Doc changes:**
- Updated `ci-pipeline.md` (added type check step, updated design decisions and consequences)
- Updated tech-debt-tracker (moved "Type checker not in CI" to Resolved)
- Updated QUALITY_SCORE.md (added type check to CI gates table, marked improvement done)
- Updated testing-methodology.md coverage count (3403 -> 3409)
- Updated PLANS.md (active -> completed v109)

**Test changes:**
- Added `TestCIWorkflowTypeChecker` to `test_docs_structure.py` (6 tests: step present, command, unresolved-import flag, invalid-method-override flag, before-tests ordering, after-ruff ordering)

3409 tests (was 3403).

---

## v110 - Claude CLI emitter hardening

Hardened `_StreamJsonEmitter` in `claude.py` with defensive error handling: added `try/finally` to `_invoke_claude` to guarantee parser flush on subprocess errors, added `isinstance(message, dict)` guard in `_process_line` for both assistant and user event types. Added 30 new tests covering non-dict message defense, `_summarize_tool` direct coverage, flush-on-error verification, and `_inject_output_format` empty cmd edge case.

**Source changes:**
- `claude.py`: Added `try/finally` around `_invoke_cli_with_cmd` in `_invoke_claude` to guarantee parser flush
- `claude.py`: Added `isinstance(message, dict)` guard in `_process_line` for assistant and user events (early return on non-dict)

**Doc changes:**
- Updated PLANS.md (active -> completed v110)
- Updated QUALITY_SCORE.md claude.py coverage notes (v110 additions)
- Updated testing-methodology.md coverage count (3409 -> 3439)

**Test changes:**
- Added `TestStreamJsonEmitterNonDictMessage` (7 tests: assistant/user with string/null/missing/list message)
- Added `TestSummarizeTool` (9 tests: Read/Edit/Write/Bash/Glob/Grep/unknown/long-cmd-truncation/missing-fields)
- Added `TestInjectOutputFormatEdgeCases` (1 test: empty cmd)
- Added `TestInvokeClaude.test_invoke_claude_flushes_parser_on_error` (1 test: flush in try/finally)
- Plus 12 additional edge case tests across existing test classes

3439 tests (was 3409).

---

## v111 - Hand World factory entrance + incinerator exit

Transformed the Hand World view from a zen garden theme to an industrial factory/incinerator theme per TODO.md requests. Replaced Torii gate entrance with a factory building (chimney, smoke, conveyor belt, windows, status light), replaced Spirit shrine exit with an incinerator (flames, embers, heat glow, exhaust chimney). Replaced zen sand plots with work desks with monitors. Updated the overall scene from zen aesthetic to industrial (tile floor, cityscape skyline, pipe clusters, equipment racks, supply crates, warning lights). Added 16 new frontend tests verifying all new DOM elements.

**Source changes:**
- `App.tsx`: Replaced `zen-torii` with `hh-factory` (building, roof, chimney, smoke x3, door, windows x2, conveyor, conveyor-lines x3, light, FACTORY label)
- `App.tsx`: Replaced `zen-shrine` with `hh-incinerator` (body, top, mouth, grate, flames x3, embers x2, heat-glow, chimney, exhaust x2, INCINERATOR label)
- `App.tsx`: Replaced `zen-plot`/`bonsai-tree` with `work-desk`/`desk-monitor` (screen, stand, base, glow)
- `App.tsx`: Updated header "Zen Garden" -> "Hand World", "Garden Status" -> "Factory Floor", "Plots" -> "Stations", aria label "garden workers" -> "factory workers"
- `styles.css`: Factory entrance CSS (~130 lines): building, roof, chimney, smoke-rise animation, door, windows with flicker, conveyor belt with scroll animation, status light with blink
- `styles.css`: Incinerator exit CSS (~150 lines): body, top, mouth, grate, flames with dance animation, embers with float animation, heat glow with pulse, chimney, exhaust
- `styles.css`: Work desk/monitor CSS (~80 lines): desk surface, monitor screen with flicker, stand, base, glow
- `styles.css`: Scene theme updates: industrial floor tiles, dark border, cityscape skyline (rectangular buildings), factory floor path, pipe cluster, equipment rack, supply crates, warning light

**Doc changes:**
- Updated PLANS.md (active -> completed v111)
- Updated FRONTEND.md (zen garden -> industrial factory description)

**Test changes:**
- Added `Hand World factory and incinerator` test suite (16 tests):
  - Hand World card header
  - FACTORY/INCINERATOR labels
  - Factory DOM elements (building/chimney/conveyor/door/roof)
  - Factory windows and status light
  - Factory smoke particles (x3)
  - Factory conveyor belt lines (x3)
  - Incinerator DOM elements (body/mouth/top/grate)
  - Incinerator flames (x3)
  - Incinerator embers and heat glow
  - Incinerator chimney and exhaust
  - Work desks replace zen plots
  - Factory Floor status summary
  - Stations count
  - Factory workers aria label
  - No old zen-torii/zen-shrine elements

169 frontend tests (was 153).

---

## v112 - Input validation hardening

Hardened input validation across three modules: differentiated `resolve_repo_target` error messages (empty/absolute → "path must be a non-empty relative path", traversal → "path escapes repository root"), added `FileNotFoundError` (exit 127) and `OSError` (exit 126) handling in `_run_command`, and added `max_iterations` bounds validation (`ge=1, le=100`) to `BuildRequest` and `ScheduleRequest`.

**Source changes:**
- `filesystem.py`: Changed `resolve_repo_target` to raise distinct `ValueError` messages for empty/absolute paths vs path traversal
- `command.py`: Added `FileNotFoundError` and `OSError` exception handlers in `_run_command`, returning `CommandResult` with exit codes 127/126
- `iterative.py`: Changed `_execute_read_requests` to forward `ValueError` message from exception instead of hardcoding `"invalid path"`
- `app.py`: Added `Field(default=6, ge=1, le=100)` to `BuildRequest.max_iterations` and `ScheduleRequest.max_iterations`

**Doc changes:**
- Updated PLANS.md (active v112)
- Updated QUALITY_SCORE.md (filesystem.py and command.py coverage notes, server/app.py test class count)
- Updated testing-methodology.md coverage count (3439 -> 3443)

**Test changes:**
- Added `TestResolveRepoTargetErrorMessages` to `test_filesystem.py` (6 tests: empty/absolute/whitespace/traversal/embedded-traversal/dot-slash-only)
- Added `TestRunCommandExecErrors` to `test_command_gaps.py` (5 tests: missing-executable-127/os-error-126/preserves-command/not-successful x2)
- Added `TestRunBashScriptErrors.test_rejects_both_sources_none` (1 test)
- Added `TestBuildRequestMaxIterations` to `test_server_app_helpers.py` (6 tests: default/accept-1/accept-100/reject-0/reject-negative/reject-over-100)
- Updated `TestResolveRepoTarget` existing tests for new error messages
- Updated `test_iterative.py::TestExecuteReadRequests` for new error message forwarding

3443 tests (was 3439).

---

## v113 - Claude CLI emitter enhancements

Expanded `_StreamJsonEmitter._summarize_tool` with 6 new tool summaries (Agent, WebFetch, WebSearch, NotebookEdit, TodoWrite, MultiTool) and added token usage tracking from result events (input_tokens, output_tokens from `usage` dict).

**Source changes:**
- `claude.py`: Added Agent/WebFetch/WebSearch/NotebookEdit/TodoWrite/MultiTool branches to `_summarize_tool`
- `claude.py`: Added `usage` dict parsing in result event handler, emitting `in=N out=N` token counts

**Doc changes:**
- Updated PLANS.md (active v113)
- Updated QUALITY_SCORE.md claude.py coverage notes (v113 additions)
- Updated testing-methodology.md coverage count (3443 -> 3457)

**Test changes:**
- Added `TestSummarizeTool.test_agent_tool` (with/without description)
- Added `TestSummarizeTool.test_web_fetch_tool`
- Added `TestSummarizeTool.test_web_search_tool` (with/without query)
- Added `TestSummarizeTool.test_notebook_edit_tool`
- Added `TestSummarizeTool.test_todo_write_tool`
- Added `TestSummarizeTool.test_multi_tool` (list/non-list)
- Added `TestStreamJsonEmitterEdgeCases.test_result_event_with_usage_tokens`
- Added `TestStreamJsonEmitterEdgeCases.test_result_event_with_usage_input_only`
- Added `TestStreamJsonEmitterEdgeCases.test_result_event_with_usage_output_only`
- Added `TestStreamJsonEmitterEdgeCases.test_result_event_with_non_dict_usage_ignored`
- Added `TestStreamJsonEmitterEdgeCases.test_result_event_with_empty_usage_dict`
- Updated `TestSummarizeTool.test_unknown_tool` (Agent -> SomeNewTool)

3457 tests (was 3443).

---

## v114 - CLI base hand test coverage improvements

Added direct unit tests for three previously indirectly-tested CLI base hand methods: `_render_command` (placeholder substitution, model injection, prompt fallback, verbose flags), `_finalize_after_run` (interrupted and normal paths), `_collect_run_output` (async chunk collection wrapper), and `_truncate_summary` (static helper).

**Source changes:**
- None (test-only and doc-only changes)

**Doc changes:**
- Updated PLANS.md (active -> completed v114)
- Updated QUALITY_SCORE.md cli/base.py coverage notes

**Test changes:**
- Added `TestRenderCommandPromptPlacement` to `test_cli_hand_base_render.py` (4 tests: placeholder, p-flag, trailing-append, no-duplicate)
- Added `TestRenderCommandModelInjection` to `test_cli_hand_base_render.py` (6 tests: auto-inject, placeholder-skip, flag-skip, equals-skip, empty-skip, provider-strip)
- Added `TestRenderCommandRepoPlaceholder` to `test_cli_hand_base_render.py` (1 test: expansion)
- Added `TestRenderCommandVerboseFlags` to `test_cli_hand_base_render.py` (3 tests: injected, not-injected, no-duplicate)
- Added `TestRenderCommandMultiplePlaceholders` to `test_cli_hand_base_render.py` (1 test: multi-placeholder token)
- Added `TestFinalizeAfterRun` to `test_cli_hand_base_finalize.py` (5 tests: interrupted, normal, truncated, not-truncated, auto-pr-false)
- Added `TestCollectRunOutput` to `test_cli_hand_base_finalize.py` (3 tests: collects, empty, newlines)
- Added `TestTruncateSummary` to `test_cli_hand_base_finalize.py` (5 tests: stripped, exact-limit, over-limit, empty, whitespace)

3485 tests (was 3457).

---

## v115 - Code quality hardening

Extracted `_build_form_redirect_query()` helper in `server/app.py` to eliminate duplicated query-string building logic (3 occurrences → 1 function). Replaced silent `contextlib.suppress(Exception)` with logged `try/except` in three locations: `base.py` `_update_pr_description`, `app.py` `_fetch_claude_usage` HTTP error body read, and `app.py` `_schedule_to_response` next-run calculation. Wrapped `shlex.split` ValueError in `cli/base.py` `_base_command()` to provide clear error messages for malformed shell expressions. Removed unused `contextlib` imports from both `base.py` and `app.py`.

**Source changes:**
- `server/app.py`: Extracted `_build_form_redirect_query()` helper, replaced 2 duplicate blocks in `enqueue_build_form`; added `logging` import and `logger`; replaced `contextlib.suppress(Exception)` with logged try/except in `_fetch_claude_usage` and `_schedule_to_response`; removed unused `contextlib` imports (top-level and local)
- `lib/hands/v1/hand/base.py`: Replaced `contextlib.suppress(Exception)` with logged try/except in `_update_pr_description`; removed unused `contextlib` import
- `lib/hands/v1/hand/cli/base.py`: Wrapped `shlex.split` in try/except ValueError in `_base_command()` to provide contextual error messages

**Doc changes:**
- Updated PLANS.md (active v115)
- Updated QUALITY_SCORE.md (base.py, server/app.py, cli/base.py coverage notes)
- Updated testing-methodology.md coverage count (3457 -> 3500)

**Test changes:**
- Added `TestBuildFormRedirectQuery` to `test_v115_code_quality.py` (9 tests: minimal fields, all optional, false booleans excluded, default ci_check_wait excluded, none pr_number excluded, empty/whitespace tools/skills excluded, none model excluded)
- Added `TestUpdatePrDescriptionLogging` (1 test: debug log on update failure)
- Added `TestScheduleToResponseLogging` (1 test: debug log on next-run failure)
- Added `TestFetchClaudeUsageErrorBodyLogging` (1 test: debug log when error body unreadable)
- Added `TestBaseCommandShlexError` (3 tests: unclosed quote, raw value in message, valid passthrough)

3500 tests (was 3485).

---

## v116 - Exception handler logging improvements

Added `logger.debug(..., exc_info=True)` to all silent `except Exception` handlers in `server/app.py` (5 locations: `_check_redis_health`, `_check_db_health`, `_check_workers_health`, `_resolve_worker_capacity`, `_get_claude_oauth_token`) and `server/celery_app.py` (2 locations in `ensure_usage_schedule`). Added `logging` import and `logger` to `celery_app.py`. Removed 5 consolidated daily files (2026-03-03 through 2026-03-07) since they are already in `Week-10.md`. Updated `TestConsolidatedPlanCoverage` and `TestConsolidated20260307` to reference `Week-10.md` instead of removed daily files.

**Source changes:**
- `server/app.py`: Added `logger.debug(...)` to `_check_redis_health`, `_check_db_health`, `_check_workers_health`, `_resolve_worker_capacity`, `_get_claude_oauth_token` exception handlers
- `server/celery_app.py`: Added `import logging` and `logger = logging.getLogger(__name__)`; added `logger.debug(...)` to both exception handlers in `ensure_usage_schedule`

**Doc changes:**
- Updated PLANS.md (active v116)
- Updated QUALITY_SCORE.md (app.py 19 test classes, celery_app.py v116 notes)
- Updated testing-methodology.md coverage count (3500 -> 3508, frontend 153 -> 169)
- Removed `docs/exec-plans/completed/2026-03-0{3,4,5,6,7}.md` (already in Week-10.md)

**Test changes:**
- Added `TestCheckRedisHealthLogging` (1 test: debug log on redis failure)
- Added `TestCheckDbHealthLogging` (1 test: debug log on db failure)
- Added `TestCheckWorkersHealthLogging` (1 test: debug log on workers failure)
- Added `TestResolveWorkerCapacityLogging` (1 test: debug log on capacity failure)
- Added `TestGetClaudeOauthTokenLogging` (2 tests: timeout, generic exception)
- Added `TestEnsureUsageScheduleLogging` (2 tests: entry-not-found, outer failure)
- Updated `TestConsolidatedPlanCoverage` to use Week-10.md (3 tests: mar 6 coverage, all days, daily files removed)
- Updated `TestConsolidated20260307` -> `TestWeek10ConsolidatedContent` (3 tests: v80, week header, mar 7 section)

3508 tests (was 3500).

---

## v117 - Defensive guards and validation hardening

Added empty-cmd guard to `_apply_verbose_flags` (prevents IndexError on empty command list), defensive message access in `LangGraphHand.run()` (handles empty/missing/None messages gracefully), and fixed `_build_failure_message` to use `_SUMMARY_CHAR_LIMIT` instead of hardcoded 2000.

**Source changes:**
- `cli/base.py`: Added `not cmd` guard to `_apply_verbose_flags` early return
- `cli/base.py`: Changed `_build_failure_message` tail slice from `[-2000:]` to `[-self._SUMMARY_CHAR_LIMIT:]`
- `langgraph.py`: Changed `run()` to use `result.get("messages") or []` with safe indexing for empty/missing/None messages

**Doc changes:**
- Updated PLANS.md (active v117)
- Updated QUALITY_SCORE.md (cli/base.py helpers v117 notes, langgraph.py v117 notes)
- Updated testing-methodology.md coverage count (3508 -> 3520)

**Test changes:**
- Added `TestApplyVerboseFlagsEmptyCmd` (3 tests: empty-cmd, single-element, not-verbose)
- Added `TestBuildFailureMessageLimit` (4 tests: custom-limit, short-output, default-6000, whitespace-strip)
- Added `TestLangGraphHandRunDefensive` (5 tests: empty-messages, missing-key, None-messages, normal, str-fallback)
- Updated `TestBuildFailureMessage.test_truncates_long_output` for 6000 limit

3520 tests (was 3508).

---

## v118 - Input validation and safety hardening

Added file size limit to `read_text_file` (checks `stat().st_size` before reading, default 10 MB), added `max_length` constraints to `BuildRequest` and `ScheduleRequest` fields (`repo_path` 500, `prompt` 50K, `model` 200, `cron_expression` 100), and added warning logging to `_float_env` for invalid environment variable values (non-numeric, non-positive).

**Source changes:**
- `filesystem.py`: Added `_MAX_FILE_SIZE_BYTES` constant (10 MB), `max_file_size` parameter to `read_text_file`, `stat().st_size` pre-check with descriptive `ValueError`
- `app.py`: Added `Field(max_length=500)` to `BuildRequest.repo_path` and `ScheduleRequest.repo_path`, `Field(max_length=50_000)` to prompts, `Field(max_length=200)` to models, `Field(max_length=100)` to `ScheduleRequest.cron_expression`
- `cli/base.py`: Added `logger.warning()` calls in `_float_env` for non-numeric and non-positive values

**Doc changes:**
- Updated PLANS.md (active -> completed v118)
- Updated QUALITY_SCORE.md (filesystem.py 37+ tests, cli/base.py v118 notes, app.py 21 test classes)
- Updated testing-methodology.md coverage count (3520 -> 3543)

**Test changes:**
- Added `TestReadTextFileMaxFileSize` to `test_v118_input_validation.py` (6 tests: reject-over-limit, accept-at-limit, error-message-sizes, default-constant, exact-boundary, one-byte-over)
- Added `TestFloatEnvLogging` to `test_v118_input_validation.py` (5 tests: non-numeric-warning, zero-warning, negative-warning, valid-no-warning, unset-no-warning)
- Added `TestBuildRequestFieldLengths` to `test_v118_server_field_limits.py` (7 tests: normal-path, oversized-path, at-limit-path, oversized-prompt, at-limit-prompt, oversized-model, at-limit-model)
- Added `TestScheduleRequestFieldLengths` to `test_v118_server_field_limits.py` (5 tests: oversized-path, oversized-prompt, oversized-model, oversized-cron, valid-request)

3543 tests (was 3520).
